# 🚀 Быстрый старт веб-интерфейса

## Шаг 1: Установка зависимостей

```bash
pip install flask jinja2
```

Или установить все зависимости:

```bash
pip install -r requirements.txt
```

## Шаг 2: Запуск веб-приложения

```bash
python web_app.py
```

Вы увидите:

```
============================================================
Video Intelligence System - Web Interface
============================================================
Artifacts directory: artifacts
Starting server at http://localhost:5000
============================================================
```

## Шаг 3: Открыть браузер

Перейдите на **http://localhost:5000**

## 📁 Структура веб-интерфейса

### Главная страница (`/`)
- Список всех обработанных видео
- Статистика по каждому видео
- Статус обработки
- Быстрый доступ к результатам

### Страница сессии (`/session/<session_id>`)

**7 вкладок с полной информацией:**

1. **Обзор** 📊
   - Общая суммаризация всего видео
   - Ключевые тезисы
   - Основные темы
   - Временная шкала

2. **Транскрипция** 📝
   - Полный текст с таймкодами
   - Информация о языке
   - Статистика транскрибации
   - Аккордеон для удобной навигации

3. **Сегменты** 📚
   - Семантические блоки
   - Суммаризация каждого сегмента
   - Полный текст (раскрывается по клику)
   - Compression ratio

4. **Суммаризация** ⚡
   - Мета-суммаризация
   - Ключевые тезисы
   - Концентрированная информация

5. **Термины** 📖
   - Технические термины с частотой
   - Персоны (NER)
   - Организации (NER)
   - Локации (NER)

6. **Вопросы** ❓
   - Автогенерированные вопросы
   - Фильтрация по сложности (легкие/средние/сложные)
   - Привязка к таймкодам
   - Контекст для каждого вопроса

7. **Статьи** 🔗
   - Рекомендуемые материалы
   - Релевантность
   - Источники (Wikipedia, Habr, и т.д.)
   - Прямые ссылки

## 🎨 Дизайн

- **Bootstrap 5** - современный и адаптивный
- **Bootstrap Icons** - красивые иконки
- **Градиенты** - яркий дизайн
- **Адаптивность** - работает на всех устройствах
- **Темная тема** для карточек
- **Анимации** при наведении

## 🔧 Настройка

### Изменить порт

В `web_app.py`:

```python
app.run(debug=True, host='0.0.0.0', port=8080)  # Вместо 5000
```

### Изменить папку с артефактами

В `web_app.py`:

```python
app.config['ARTIFACTS_DIR'] = Path('/путь/к/вашим/artifacts')
```

## 📊 API Endpoints

Веб-интерфейс также предоставляет JSON API:

### Получить список сессий
```bash
curl http://localhost:5000/api/sessions
```

### Получить данные конкретной сессии
```bash
curl http://localhost:5000/api/session/video_20241116_120000
```

## 🐛 Troubleshooting

### Ошибка: "No module named 'flask'"

```bash
pip install flask
```

### Ошибка: "Session not found"

Убедитесь, что:
1. Папка `artifacts/` существует
2. В ней есть обработанные видео
3. В каждой папке есть `checkpoint.json`

### Пустая главная страница

Обработайте хотя бы одно видео:

```bash
python -m src.cli process-all your_video.mp4
```

## 💡 Полезные советы

1. **Используйте полный пайплайн** для лучших результатов:
   ```bash
   python -m src.cli process-all video.mp4 --language ru
   ```

2. **Проверяйте checkpoint.json** для отладки

3. **Веб-интерфейс автоматически обновляется** при добавлении новых видео

4. **Используйте Chrome/Firefox** для лучшей совместимости

---

**Наслаждайтесь использованием! 🎉**
